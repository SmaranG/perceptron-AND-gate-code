# Perceptron AND Gate
This implements a simple **Perceptron** in Python to model the logical **AND gate**.

## Implementation Details
- Language: Python
- Library used: NumPy
- Learning rate: 0.1
- Activation function: Step function
- Bias term included

## Files
- `perceptron.py`

## How to Run
1. Make sure Python is installed.
2. Install NumPy if not already installed:
